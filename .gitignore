# Secrets / local configs
config.json
dpmp/config.json
dpmp/config.json.*
*.env

# Logs
*.log
dpmpv2_run.log
dpmpv2_gui.log

# Backups / scratch / dumps
*.BAK*
*.bak*
*.KNOWN_GOOD*
*.SAFE_RESTORE*
*.BEFORE_*
*.TRUNCATED*
*.old

# Local docs (don’t track)
RUNBOOK*.md
HANDOFF*.md

# One-off tooling / scratch (don’t track)
exact_replace.py
tools/
dpmp.py
dpmp/test_miner.py

# Docker artifacts (we’re back to non-docker)
dpmp/Dockerfile
gui/Dockerfile

# Python cache
__pycache__/
*.pyc

# Old v1 artifacts / backups we don’t track
dpmp/config.example.json
dpmp/dpmp_old_version.py

# Rotated / archived logs
dpmpv2_run.log.*

# VS Code (local)
.vscode/
*.code-workspace

# runtime config (DO NOT COMMIT)
dpmp/config_v2.json
